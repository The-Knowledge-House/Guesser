function fight(){
  let hero = prompt("Choose your hero:");
    console.log("Hero" + hero)
  let monster = prompt("Name the monster:");
    console.log("Monster" + monster)

  let heroHealth = 50
  let villainHealth = 50
  while (heroHealth && villainHealth > 0) {

  }
}
